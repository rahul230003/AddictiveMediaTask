<!DOCTYPE html>
<html>
    <head>
        <title>Success</title>
    </head>
    <body>
        <% for(var i=0;i<data.length;i++){%>
        <h1><%=data[i].name%></h1>
        <p><%=data[i].country%></p>
        <% } %>
    </body>
</html>